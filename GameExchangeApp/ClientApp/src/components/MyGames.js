import React from 'react'
import Button from './Button'

const AddGame = () => {
    return (
        <Button title={'Add Game'} color={{ backgroundColor: 'green' }} onClick={''} />

    )
}

export default AddGame
